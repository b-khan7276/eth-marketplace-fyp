#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../typescript-json-schema/bin/typescript-json-schema" "$@"
else 
  exec node  "$basedir/../typescript-json-schema/bin/typescript-json-schema" "$@"
fi
